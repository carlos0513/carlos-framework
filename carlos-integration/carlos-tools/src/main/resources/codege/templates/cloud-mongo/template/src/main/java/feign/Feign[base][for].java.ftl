package ${project.packageName}.feign;

/**
 * <p>
 * ${table.comment} feign 提供接口
 * </p>
 *
 * @author  ${project.author}
 * @date    ${.now}
 */
// @FeignClient("${project.artifactId}")
public interface Feign${table.classPrefix} {


}
