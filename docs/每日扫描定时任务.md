# Carlos Framework 每日代码扫描定时任务

你是 Carlos Framework 专属开发助手。请执行每日项目全面扫描任务，深度分析代码质量、设计缺陷、功能完善度、可优化点，以及*
*已依赖技术组件的特性利用情况**。

---

## 任务目标

对 `carlos-framework` 项目进行**全量代码扫描**，生成《每日代码质量与优化报告》，保存到：

```
reports/daily-scan-report-$(date +%Y-%m-%d-%H%M).md
```

---

## 执行步骤

### 1. 代码同步与环境准备

```bash
cd /home/carlos/.openclaw/agents/carlos-dev-assistant/workspace/carlos-framework
# 先丢弃可能的本地修改，确保干净状态
git checkout -- . 2>/dev/null || true
# 尝试拉取最新代码（网络可能不稳定）
git pull origin main 2>/dev/null || echo "[WARN] git pull 失败，使用本地代码继续扫描"
git rev-parse HEAD
git log --oneline -5
# 统计代码规模
echo "=== 代码规模统计 ==="
find . -name '*.java' -not -path '*/target/*' | wc -l
echo "Java 文件数"
find . -name '*.xml' -not -path '*/target/*' | wc -l
echo "XML 文件数"
```

---

### 2. 全量模块扫描范围

**必须扫描的所有模块（基于项目实际存在的全部模块，按层级排序）：**

#### 基础设施层

| 模块                    | 扫描重点                                   |
|-----------------------|----------------------------------------|
| `carlos-dependencies` | BOM 依赖版本矩阵、版本冲突、过期依赖、已知 CVE            |
| `carlos-parent`       | 插件版本、构建配置、flatten-maven-plugin 配置、分发管理 |

#### Commons 通用基础层

| 模块                   | 扫描重点                                         |
|----------------------|----------------------------------------------|
| `carlos-commons` (父) | 模块聚合配置、依赖管理一致性                               |
| `carlos-utils`       | 工具类设计缺陷、重复造轮子、线程安全、空指针风险、与 Hutool/Guava 功能重叠 |

#### Spring Boot Starters 层（核心框架）

| 模块                                       | 扫描重点                                                                   |
|------------------------------------------|------------------------------------------------------------------------|
| `carlos-spring-boot` (父)                 | Starter 聚合完整性、版本一致性                                                    |
| `carlos-spring-boot-core`                | Result/ApiResponse 响应体系、UserInfo 上下文、异常体系、分页工具、BaseEnum、IdUtils、统一响应规范 |
| `carlos-spring-cloud-starter`            | Spring Cloud Alibaba 集成、Nacos 配置、负载均衡、服务发现                             |
| `carlos-spring-boot-starter-web`         | XSS 过滤器、请求包装器、CORS 配置、全局异常处理、参数校验、Jackson 配置                           |
| `carlos-spring-boot-starter-mybatis`     | MyBatis-Plus 配置、分页插件、ID 生成器、多数据源、SQL 注入风险、mybatis-plus-join 使用         |
| `carlos-spring-boot-starter-security`    | SecurityFilterChain 配置、权限评估器、方法级安全注解 `@PreAuthorize`、缓存同步              |
| `carlos-spring-boot-starter-encrypt`     | SM2/SM4 国密算法实现、密钥管理、加密模式、性能优化、与 Spring Boot 3 兼容性                      |
| `carlos-spring-boot-starter-minio`       | 文件上传限制、断点续传、预签名 URL、Bucket 策略、大文件分片                                    |
| `carlos-spring-boot-starter-flowable`    | 流程引擎配置、数据库表膨胀、异步任务、历史数据清理、与 Spring Boot 3 兼容性                          |
| `carlos-spring-boot-starter-ai`          | ChatMemory 内存管理、工具调用、流式响应、超时配置、Token 限制                                |
| `carlos-spring-boot-starter-apm`         | SkyWalking 集成、Trace 上下文传递、自定义标签、性能指标上报                                 |
| `carlos-spring-boot-starter-datacenter`  | 多租户数据隔离、租户上下文传递、动态数据源切换                                                |
| `carlos-spring-boot-starter-datascope`   | 数据权限范围控制、SQL 拦截、部门/岗位/个人级权限                                            |
| `carlos-spring-boot-starter-disruptor`   | RingBuffer 配置、生产者/消费者模式、异常处理、背压策略                                      |
| `carlos-spring-boot-starter-integration` | Spring Integration 通道、消息路由、转换器、网关配置                                    |
| `carlos-spring-boot-starter-json`        | Fastjson2/Jackson 配置、序列化安全、日期格式、枚举序列化                                  |
| `carlos-spring-boot-starter-log`         | 日志链路追踪、MDC 上下文、异步日志、日志脱敏、日志归档策略                                        |
| `carlos-spring-boot-starter-migration`   | Flyway/Liquibase 迁移脚本、版本控制、多环境迁移策略                                     |
| `carlos-spring-boot-starter-mongodb`     | MongoTemplate 配置、连接池、索引管理、事务支持、GridFS                                  |
| `carlos-spring-boot-starter-mq`          | RabbitMQ/Kafka/RocketMQ 配置、生产者确认、消费者幂等、死信队列、消息追踪                       |
| `carlos-spring-boot-starter-openapi`     | Knife4j/OpenAPI 配置、文档分组、接口鉴权展示、离线文档导出                                  |
| `carlos-spring-boot-starter-oss`         | 阿里云/腾讯云/华为云 OSS 适配、断点续传、STS 临时凭证、CDN 集成                                |
| `carlos-spring-boot-starter-sms`         | 短信厂商多路适配、模板管理、发送限流、验证码防刷、发送记录                                          |
| `carlos-spring-boot-starter-snowflake`   | 雪花算法 WorkerId 分配、时钟回拨处理、ID 反解                                          |
| `carlos-spring-boot-starter-translation` | 国际化配置、动态语言切换、翻译源适配、缓存策略                                                |

#### Redis 缓存层（子聚合器）

| 模块                                          | 扫描重点                                                    |
|---------------------------------------------|---------------------------------------------------------|
| `carlos-spring-boot-redis` (父)              | Redis Starter 聚合配置                                      |
| `carlos-spring-boot-starter-redis`          | Redisson 配置、缓存策略、多级缓存一致性 (Caffeine+Redis)、Key 命名规范、过期策略 |
| `carlos-spring-boot-starter-redis-core`     | Redis 核心工具类、序列化、连接池、分布式锁实现                              |
| `carlos-spring-boot-starter-redis-reactive` | Reactive Redis 支持、Mono/Flux 链式调用、背压处理                   |

#### Integration 业务集成层

| 模块                       | 扫描重点                                      |
|--------------------------|-------------------------------------------|
| `carlos-integration` (父) | 业务模块聚合配置、依赖方向合规性                          |
| `carlos-gateway`         | OAuth2 过滤器、WAF、防重放、令牌校验、限流熔断、路由配置、跨域处理    |
| `carlos-tools`           | GUI 桌面工具代码质量、Swing 线程安全、代码生成器模板、GitLab 集成 |

#### Audit 审计日志模块

| 模块                   | 扫描重点                                            |
|----------------------|-------------------------------------------------|
| `carlos-audit` (父)   | 模块聚合完整性                                         |
| `carlos-audit-api`   | Feign 接口定义、审计事件 AO/Param、FallbackFactory        |
| `carlos-audit-boot`  | 自动配置、审计监听器、Starter 注册                           |
| `carlos-audit-bus`   | ClickHouse 批量写入、Disruptor 配置、审计查询性能、数据保留策略、异步消费 |
| `carlos-audit-cloud` | Feign 接口实现、REST 端点、服务间调用降级                      |

#### Auth 认证授权模块

| 模块                    | 扫描重点                                             |
|-----------------------|--------------------------------------------------|
| `carlos-auth` (父)     | 模块聚合完整性                                          |
| `carlos-auth-api`     | Feign 接口定义、认证相关 AO/Param、FallbackFactory         |
| `carlos-auth-boot`    | 自动配置、认证相关 Starter 注册                             |
| `carlos-auth-cloud`   | Feign 接口实现、REST 端点、Token 中继、服务间调用降级              |
| `carlos-auth-service` | OAuth2 授权服务器、密码编码器、Token 管理、暴力破解防护、JWT 配置、用户详情服务 |

#### License 软件授权模块

| 模块                                            | 扫描重点                                    |
|-----------------------------------------------|-----------------------------------------|
| `carlos-license` (父)                          | 模块聚合完整性、**生产环境严禁包含 license-generate**   |
| `carlos-license-core`                         | 证书验证逻辑、License 内容校验、TrueLicense 集成、公钥安全 |
| `carlos-spring-boot-starter-license-generate` | **仅开发环境使用**，私钥管理安全                      |
| `carlos-spring-boot-starter-license-verify`   | 自动配置、证书加载、过期处理、验证拦截、Starter 注册          |

#### Message 消息通知模块

| 模块                     | 扫描重点                                     |
|------------------------|------------------------------------------|
| `carlos-message` (父)   | 模块聚合完整性                                  |
| `carlos-message-api`   | Feign 接口定义、消息相关 AO/Param、FallbackFactory |
| `carlos-message-boot`  | 自动配置、消息模板管理、渠道适配 Starter 注册              |
| `carlos-message-bus`   | 消息发送业务逻辑、多渠道路由、模板渲染、发送记录、限流防刷            |
| `carlos-message-cloud` | Feign 接口实现、REST 端点、服务间调用降级               |
| `carlos-message-core`  | 消息领域模型、渠道抽象、模板引擎、消息队列集成                  |

#### Org 组织架构模块

| 模块                 | 扫描重点                                     |
|--------------------|------------------------------------------|
| `carlos-org` (父)   | 模块聚合完整性                                  |
| `carlos-org-api`   | Feign 接口定义、组织相关 AO/Param、FallbackFactory |
| `carlos-org-boot`  | 自动配置、部门缓存初始化、Starter 注册                  |
| `carlos-org-bus`   | 部门缓存策略、Redis 缓存一致性、树形结构递归性能、数据权限、岗位/人员关系 |
| `carlos-org-cloud` | Feign 接口实现、REST 端点、服务间调用降级               |

#### System 系统管理模块

| 模块                    | 扫描重点                                     |
|-----------------------|------------------------------------------|
| `carlos-system` (父)   | 模块聚合完整性                                  |
| `carlos-system-api`   | Feign 接口定义、系统相关 AO/Param、FallbackFactory |
| `carlos-system-boot`  | 自动配置、字典缓存、参数管理、Starter 注册                |
| `carlos-system-bus`   | 字典/参数缓存策略、配置热更新、定时任务管理、操作日志              |
| `carlos-system-cloud` | Feign 接口实现、REST 端点、服务间调用降级               |

#### Samples 示例与测试层

| 模块                         | 扫描重点                                   |
|----------------------------|----------------------------------------|
| `carlos-samples` (父)       | 示例模块聚合完整性                              |
| `carlos-sample-apm`        | APM 接入示例、Trace 演示、性能监控示例完整性            |
| `carlos-sample-cloud`      | Spring Cloud 微服务调用示例、Feign 降级示例、配置中心示例 |
| `carlos-sample-datacenter` | 多租户示例、动态数据源切换示例、租户隔离演示                 |
| `carlos-sample-datascope`  | 数据权限示例、不同角色数据范围演示                      |
| `carlos-sample-disruptor`  | Disruptor 使用示例、高性能队列演示                 |
| `carlos-sample-docking`    | 第三方对接示例、Webhook 接收、签名验证                |
| `carlos-sample-encrypt`    | 国密加解密示例、SM2/SM4 使用演示、密钥配置示例            |
| `carlos-sample-flowable`   | 工作流定义示例、流程部署、任务审批演示                    |
| `carlos-sample-log`        | 日志链路示例、MDC 使用、日志脱敏演示                   |
| `carlos-sample-minio`      | 文件上传/下载示例、分片上传、预签名 URL 演示              |
| `carlos-sample-mongodb`    | MongoDB CRUD 示例、聚合查询、GridFS 演示         |
| `carlos-sample-mq`         | 消息队列生产消费示例、延时消息、顺序消息、事务消息              |
| `carlos-sample-mybatis`    | MyBatis-Plus 使用示例、多数据源、动态表名、Join 查询    |
| `carlos-sample-openapi`    | 接口文档示例、分组配置、自定义注解演示                    |
| `carlos-sample-oss`        | 云存储对接示例、STS 临时凭证、分片上传                  |
| `carlos-sample-redis`      | Redis 缓存示例、分布式锁、限流、排行榜、Geo             |
| `carlos-sample-sms`        | 短信发送示例、验证码场景、模板变量                      |
| `carlos-sample-snowflake`  | 分布式 ID 生成示例、ID 反解、批量生成                 |
| `carlos-sample-web`        | Web 基础示例、参数校验、全局异常、文件上传/下载             |
| `carlos-test`              | 完整集成测试、全链路测试、回归测试、配置合理性                |

> **扫描原则**：以上所有模块必须逐一扫描，不允许跳过任何模块。父 POM 关注依赖管理与构建配置，子模块关注代码质量与业务逻辑。

---

### 3. 扫描维度与检查清单

#### 3.1 设计缺陷扫描（🔴 最高优先级）

- [ ] **分层架构违规**：Service 层是否直接引用 Mapper？Controller 是否包含业务逻辑？API 层是否反向依赖 Business 模块？
- [ ] **领域模型污染**：Entity 是否被直接返回给前端？VO/DTO/AO 是否混用？是否出现跨层传输的裸 Map/JSONObject？
- [ ] **接口设计缺陷**：Manager 接口与实现是否分离？Service 是否返回 VO？Convert 是否使用 MapStruct？
- [ ] **配置设计缺陷**：是否使用 `@Value` 注入（应使用 `@ConfigurationProperties`）？配置类是否缺少
  `@ConditionalOnProperty`？
- [ ] **异常体系缺陷**：是否抛出 `RuntimeException`？是否硬编码错误码？异常是否被吞掉（空 catch）？
- [ ] **自动配置缺陷**：`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 是否完整？配置顺序是否正确？
- [ ] **循环依赖**：模块间是否存在循环依赖？Bean 间是否存在循环依赖？
- [ ] **扩展性缺陷**：是否缺少 SPI 扩展点？是否硬编码实现类？策略模式是否合理使用？
- [ ] **包结构缺陷**：包命名是否符合 `com.carlos.{service}` 规范？领域模型包是否按 param/dto/vo/ao/entity/enums/excel 分层？

#### 3.2 功能完善度扫描（🟡 高优先级）

- [ ] **核心功能缺失**：Starter 模块是否缺少关键配置属性类？是否缺少 `spring.factories` 或 `.imports` 注册？
- [ ] **健康检查缺失**：是否缺少 Spring Boot Actuator 健康检查？自定义 HealthIndicator 是否实现？
- [ ] **监控埋点缺失**：关键方法是否缺少 Micrometer 指标？异步任务是否缺少链路追踪？
- [ ] **参数校验缺失**：Controller 入参是否缺少 `@Valid`？自定义校验注解是否完善？
- [ ] **文档缺失**：公开 API 是否缺少 Swagger/Knife4j 注解？复杂业务方法是否缺少 JavaDoc？
- [ ] **测试缺失**：核心逻辑是否缺少单元测试？集成测试是否覆盖主流程？测试是否使用自动回滚？
- [ ] **降级熔断缺失**：Feign 接口是否都有 FallbackFactory？Redis 故障时是否有降级策略？
- [ ] **国际化缺失**：错误消息是否硬编码中文？是否支持 i18n？
- [ ] **多租户缺失**：多租户模块是否完整？租户隔离是否覆盖所有数据操作？
- [ ] **示例缺失**：每个 Starter 是否都有对应的 `carlos-sample-xxx` 示例模块？示例是否覆盖核心功能？

#### 3.3 代码可优化点扫描（🟢 中优先级）

- [ ] **性能优化**：N+1 查询问题、大表缺少索引提示、循环内 RPC 调用、Redis 大 Key、慢 SQL
- [ ] **并发优化**：共享可变状态、锁粒度太大、线程池参数不合理、CompletableFuture 使用不当
- [ ] **内存优化**：大对象常驻内存、缓存无限增长、流未关闭、toArray(new T[0]) 优于 toArray(new T[size])
- [ ] **算法优化**：树形结构递归深度过大、List.contains() 遍历查找（应使用 Set）、字符串拼接（应使用 StringBuilder）
- [ ] **Spring Boot 最佳实践**：`@Autowired` 字段注入（应使用构造器注入）、`@PostConstruct` 替代方案、不必要的
  `@ComponentScan`
- [ ] **日志优化**：日志参数未使用占位符、DEBUG 日志未加 `isDebugEnabled()`、异常堆栈丢失
- [ ] **代码重复**：工具类方法重复（与 Hutool/Guava 重叠）、相似逻辑未抽取、魔法值硬编码

#### 3.4 安全检查清单（🔴 最高优先级）

- [ ] 是否存在 MD5/SHA1 等弱密码编码器
- [ ] ObjectMapper Visibility 配置是否过宽（ANY → 应 PUBLIC_ONLY）
- [ ] Redis 中是否存储敏感 Token 明文
- [ ] WebClient/RestTemplate 是否配置超时和重试
- [ ] 静态工具类（RedisUtil 等）是否导致难以测试
- [ ] 匿名内部类是否影响 Native Image 编译（应使用 lambda 或独立类）
- [ ] Map/Cache 是否有内存泄漏风险（无过期清理策略）
- [ ] SQL 注入风险：`${}` 拼接、动态表名/列名未白名单校验
- [ ] 敏感配置是否硬编码（密码、密钥、AK/SK）
- [ ] 文件上传是否限制类型和大小、路径遍历风险
- [ ] 反序列化安全风险：是否校验类白名单
- [ ] **License 安全**：生产环境是否误包含 `carlos-spring-boot-starter-license-generate`
- [ ] **国密安全**：SM2/SM4 密钥是否硬编码或存储在配置文件明文

#### 3.5 依赖与技术债务扫描

- [ ] 依赖版本是否严重落后（与当前最新稳定版差距 > 2 个小版本）
- [ ] 是否存在已知 CVE 漏洞（搜索 `https://osv.dev/` 或 Maven Central Security）
- [ ] 是否存在已弃用的 API 调用（Spring Boot 3.x 迁移残留）
- [ ] `javax.*` 包是否还有残留（应全部迁移到 `jakarta.*`）
- [ ] 是否存在 SNAPSHOT 依赖用于生产环境

---

### 4. 深度代码分析要求

对每个发现的问题，必须提供以下信息：

1. **问题定位**：文件路径 + 行号范围（精确到方法或代码块）
2. **相关代码**：完整的代码片段（不少于 5 行上下文）
3. **详细分析**：
    - 问题类型（设计缺陷 / 功能缺失 / 可优化 / 安全漏洞 / 技术债务）
    - 严重程度（P0-Critical / P1-High / P2-Medium / P3-Low）
    - 影响范围（单个方法 / 单个类 / 整个模块 / 框架级）
    - 触发条件（什么场景下会出问题）
    - 潜在风险（性能下降 / 安全漏洞 / 功能异常 / 维护困难）
4. **解决方案**：
    - 推荐修复方案（提供具体代码示例）
    - 短期规避措施（如无法立即修复）
    - 长期改进建议（架构层面）
    - 参考文档或最佳实践链接

---

### 5. 组件特性利用分析（⭐ 新增核心步骤）

**这是本次扫描最重要的增值分析。** 目标是确保框架已经依赖的技术组件，其最新/高级特性被充分利用，避免 "有车不开、有宝不识"。

#### 5.1 分析范围

基于项目 `carlos-dependencies/pom.xml` 和 `carlos-parent/pom.xml` 中声明的全部技术栈，重点分析以下组件：

| 技术组件                     | 当前版本（参考）   | 特性利用分析重点                                                                              |
|--------------------------|------------|---------------------------------------------------------------------------------------|
| **Spring Boot**          | 3.5.9      | 虚拟线程、GraalVM Native Image、结构化日志、Observability、RestClient、HTTP Interface、ProblemDetail |
| **Spring Cloud**         | 2025.0.1   | LoadBalancer 自适应、Gateway 新过滤器、Config 数据导入、服务契约                                        |
| **Spring Cloud Alibaba** | 2025.0.0.0 | Nacos 2.x 长连接、MSE 服务治理、Sentinel 网关流控、Seata Saga 模式                                    |
| **MyBatis-Plus**         | 3.5.15     | Lambda 查询链、Active Record 模式、多租户插件、动态表名、Join 扩展                                        |
| **Seata**                | 2.0.0      | Saga 状态机模式、AT 模式优化、XA 模式、TCC 模式、事务分组                                                  |
| **Redisson**             | (BOM 管理)   | RLocalCachedMap 多级缓存、RDelayedQueue 延时队列、RLock 看门狗、RTopic 发布订阅                         |
| **Hutool**               | 5.8.40     | 新工具类（BeanUtil/ReflectUtil）、验证码、身份证校验、敏感信息脱敏、Cron 表达式                                  |
| **MapStruct**            | 1.6.3      | 构造函数映射、Builder 映射、前置/后置处理、SPI 扩展、Spring 依赖注入                                          |
| **Guava**                | 33.4.8-jre | CacheBuilder 自动回收、RateLimiter、EventBus、Table 结构、StopWatch                             |
| **Druid**                | 1.2.27     | 监控页面、SQL 防注入 WallFilter、多数据源、连接池动态调整                                                  |
| **SkyWalking**           | 9.5.0      | 自定义 Span、标签、日志关联、性能剖析 Profiling、告警规则                                                  |
| **Fastjson**             | 2.0.60     | JSONB 格式、autoType 安全白名单、JSONPath、日期格式化                                                |
| **Apache POI**           | 5.2.5      | SXSSF 流式大 Excel、数据验证、公式计算、内存优化                                                        |
| **Knife4j**              | 4.5.0      | Gateway 聚合文档、增强排序、离线文档 Markdown、Authorize 全局参数                                        |
| **Flowable**             | (BOM 管理)   | 事件监听器、历史数据清理、异步执行器、DMN 决策表、CMMN 案例管理                                                  |
| **MinIO Java Client**    | (BOM 管理)   | 分片上传、服务端加密、对象锁定、生命周期管理、监听事件                                                           |
| **Disruptor**            | (BOM 管理)   | 多生产者模式、WorkerPool、EventTranslator、等待策略优化                                              |
| **MongoDB Driver**       | (BOM 管理)   | Change Streams、聚合管道、文本搜索、地理空间查询、事务                                                    |
| **RabbitMQ/Kafka**       | (BOM 管理)   | 死信队列、延时消息、批量消费、事务消息、Exactly-Once 语义                                                   |
| **TrueLicense**          | (BOM 管理)   | 自定义 License 内容、硬件指纹绑定、License 模板、XML/JSON 序列化                                         |
| **Caffeine**             | (BOM 管理)   | W-TinyLFU 淘汰策略、异步加载、刷新策略、统计信息、监听器                                                     |

#### 5.2 分析执行方法

对每个组件执行以下步骤：

1. **搜索最新特性**：使用 web_search 搜索该组件最新版本的 Release Notes、新特性介绍、最佳实践
2. **代码扫描匹配**：在项目代码中搜索该组件相关的配置和用法，判断以下问题：
    - ❓ **是否使用了基础功能，而忽略了高级特性？**（如只用了 Redis String，没用 Redisson 的分布式集合/队列）
    - ❓ **是否有更好的替代写法？**（如手动分页 → MyBatis-Plus 分页插件；手动锁 → Redisson 分布式锁）
    - ❓ **是否存在性能更优的特性未启用？**（如 POI 的 SXSSF 流式写入大 Excel；Caffeine 的异步加载）
    - ❓ **是否有安全增强特性未使用？**（如 Fastjson autoType 白名单；Druid WallFilter）
    - ❓ **是否有可简化代码的特性？**（如 MapStruct 的构造函数映射；Spring Boot 的 HTTP Interface）
3. **输出利用建议**：针对每个组件，输出：
    - **当前使用方式**（从代码中扫描到的实际用法）
    - **未被利用的潜力特性**（列出 2~5 个最适合本项目的高级特性）
    - **推荐应用场景**（具体到哪个模块、哪个功能点可以使用）
    - **预期收益**（性能提升 / 代码简化 / 安全增强 / 功能增强）
    - **实施难度**（低/中/高，附带代码示例）

---

### 6. 报告生成格式

报告文件：`reports/daily-scan-report-YYYY-MM-DD-HHMM.md`

报告结构：

```markdown
# Carlos Framework 每日代码质量与优化报告

**生成时间**: YYYY-MM-DD HH:MM  
**扫描 Commit**: xxx  
**扫描范围**: 全量模块（约 X 个 Java 文件，Y 个 XML 文件）  
**发现问题总数**: N 个（P0: a 个, P1: b 个, P2: c 个, P3: d 个）  
**组件特性利用建议**: M 条

---

## 一、执行摘要

### 1.1 今日变更概览
- Git 拉取状态: 成功/失败
- Commit: xxx
- 变更文件数: N 个
- 新增代码: +X 行 / 删除代码: -Y 行

### 1.2 问题分布总览

| 问题类型 | P0-Critical | P1-High | P2-Medium | P3-Low | 合计 |
|---------|-------------|---------|-----------|--------|------|
| 设计缺陷 | | | | | |
| 功能缺失 | | | | | |
| 性能优化 | | | | | |
| 安全漏洞 | | | | | |
| 技术债务 | | | | | |

### 1.3 组件特性利用概览

| 组件 | 当前使用深度 | 待利用关键特性数 | 优先级推荐 |
|------|-------------|-----------------|-----------|
| Spring Boot | 基础 | 3 | 高 |
| Redisson | 基础 | 2 | 高 |
| MyBatis-Plus | 中等 | 1 | 中 |
| ... | | | |

### 1.4 今日重点关注
- [TOP 1] xxx（P0，影响 xxx）
- [TOP 2] xxx（P1，影响 xxx）
- [TOP 3] xxx（P1，影响 xxx）
- [TOP 特性] 建议启用 xxx 的 yyy 特性（收益 xxx）

---

## 二、设计缺陷详情

### 问题 1: [简短标题]

**模块**: `carlos-xxx`  
**文件**: `carlos-xxx/src/main/java/.../Xxx.java`  
**行号**: L45-L78  
**严重程度**: P0/P1/P2/P3  
**影响范围**: 框架级/模块级/类级

#### 相关代码

// carlos-xxx/src/main/java/.../Xxx.java L45-L78
[此处粘贴完整代码片段]


#### 详细分析

- **问题描述**: ...
- **违反规范**: （引用 AGENTS.md 或 SKILL.md 中的具体规范）
- **触发条件**: ...
- **潜在风险**: ...

#### 解决方案

**推荐修复**:

[提供修复后的代码示例]


**短期规避**: ...（如无法立即修复）

**长期建议**: ...（架构层面改进）

**参考文档**: [链接]

---

### 问题 2: [简短标题]

...[同上格式]...

---

## 三、功能未完善详情

### 问题 N: [简短标题]

...[同上格式]...

---

## 四、代码可优化点详情

### 问题 M: [简短标题]

...[同上格式]...

---

## 五、安全漏洞详情

### 问题 K: [简短标题]

...[同上格式，安全类问题必须提供 CWE 编号和修复优先级]...

---

## 六、依赖与技术债务

### 6.1 过期依赖清单

| 依赖 | 当前版本 | 最新稳定版 | 差距 | CVE | 建议操作 |
|----|------|-------|----|-----|------|
|    |      |       |    |     |      |

### 6.2 已弃用 API 使用

| 文件 | 行号 | 已弃用 API | 替代方案 |
|----|----|---------|------|
|    |    |         |      |

---

## 七、组件特性利用分析（⭐ 核心章节）

> 本章节分析框架已依赖的技术组件中，哪些高级/新特性尚未被充分利用，并提出具体的应用建议。

### 7.1 高优先级组件

#### 组件 1: {组件名} (当前版本: x.x.x)

**当前项目使用概况**

- 使用位置: ...
- 当前用法摘要: ...

**未被充分利用的特性**

| 特性名称 | 当前状态 | 推荐应用场景 | 预期收益 | 实施难度 |
|------|------|--------|------|------|
|      |      |        |      |      |

**具体应用建议**

**建议 1: ...**

- **场景**: ...
- **代码示例**: ...
- **收益**: ...

#### 组件 2: ...

...[同上格式]...

### 7.2 中优先级组件

### 7.3 待评估组件

（新发现的技术组件，框架尚未依赖但可能有用，可在此列出供评估）

---

## 八、模块健康度评分

| 模块                      | 设计规范  | 功能完整  | 代码质量  | 安全性   | 组件利用  | 综合评分    |
|-------------------------|-------|-------|-------|-------|-------|---------|
| carlos-spring-boot-core | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ | 4.8/5.0 |
| ...                     |       |       |       |       |       |         |

> 评分标准：5星=优秀，4星=良好，3星=一般，2星=较差，1星=需立即整改

---

## 九、待办事项与修复计划

### 9.1 今日新增待办

- [ ] [P0] carlos-xxx: 修复 xxx 问题（指派: 建议 @xxx）
- [ ] [P1] carlos-xxx: 完善 xxx 功能（指派: 建议 @xxx）
- [ ] [P2] carlos-xxx: 优化 xxx 实现（指派: 建议 @xxx）
- [ ] [特性] carlos-xxx: 引入 xxx 的 yyy 特性（指派: 建议 @xxx）

### 9.2 历史遗留跟踪

（复制昨日报告中未关闭的待办，标记进度）

- [ ] [P0] carlos-xxx: xxx 问题 ⏳ 修复中 / ✅ 已修复 / ❌ 搁置

---

## 十、技术趋势洞察（可选）

### 10.1 关键依赖版本动态

| 组件          | 框架版本  | 最新版本  | 发布日期       | 升级建议     |
|-------------|-------|-------|------------|----------|
| Spring Boot | 3.5.9 | x.x.x | YYYY-MM-DD | 建议/观望/必须 |

### 10.2 行业最佳实践更新

（如有发现与框架相关的新的安全通告、Spring 官方推荐实践等）

---

*本报告由 OpenClaw 定时任务自动生成，问题定位基于静态代码分析，具体修复前请人工复核。*
```

---

### 7. 报告提交

扫描完成后，执行：

```bash
cd /home/carlos/.openclaw/agents/carlos-dev-assistant/workspace/carlos-framework
git add reports/
git commit -m "docs: 每日代码质量与优化报告 $(date +%Y-%m-%d-%H%M)"
git push origin main 2>/dev/null || echo "[WARN] git push 失败，请手动提交报告"
```

---

## 扫描规范与约束

### 代码片段规范

- 引用的代码必须**包含完整的方法签名或类定义上下文**
- 必须标注**精确的文件路径和行号范围**
- 代码片段使用 ```java 语法高亮
- 若涉及多文件关联，需分别列出

### 问题分级标准

| 级别              | 定义                   | 响应时效     | 示例                             |
|-----------------|----------------------|----------|--------------------------------|
| **P0-Critical** | 会导致系统故障、数据丢失、安全漏洞的问题 | 24 小时内修复 | SQL 注入、内存泄漏、硬编码密钥、空指针崩溃        |
| **P1-High**     | 影响核心功能、性能严重下降、违反强制规范 | 本周内修复    | Service 直接调 Mapper、未加事务、缓存雪崩风险 |
| **P2-Medium**   | 影响代码质量、可维护性、存在潜在风险   | 本月内修复    | 魔法值硬编码、日志占位符缺失、重复代码            |
| **P3-Low**      | 轻微优化建议、风格问题、文档缺失     | 排期优化     | JavaDoc 缺失、变量命名不规范、import 未优化  |

### 分析深度要求

- **每个 P0/P1 问题**必须提供：代码片段 + 详细分析 + 完整解决方案
- **每个 P2 问题**必须提供：代码片段 + 分析 + 修复建议
- **每个 P3 问题**至少提供：问题描述 + 优化建议
- **每个组件特性建议**必须提供：当前用法扫描结果 + 具体应用位置 + 代码示例 + 预期收益
- 单个模块发现超过 5 个问题，需单独输出子章节

### 组件特性分析约束

- **必须基于实际代码扫描**：不允许虚构项目对组件的使用方式
- **必须结合版本特性**：搜索该组件当前依赖版本的 Release Notes，不盲目推荐超前版本特性
- **必须有具体应用场景**：不允许泛泛而谈，必须指出 "在 carlos-xxx 模块的 yyy 功能中可以使用"
- **必须评估投入产出**：对每条特性建议标注实施难度和预期收益
- **优先推荐 "低投入高回报" 的特性**：如配置项开启、注解替换、API 升级等

### 特殊场景处理

- **无新 Commit**: 仍需基于当前代码全量扫描，报告标注 "今日无代码变更，基于最新 Commit xxx 扫描"
- **新增模块**: 优先深度扫描新增模块，标记为 "新增模块重点审查"
- **大规模重构**: 若发现某模块正在被大规模重构，可标注 "该模块处于重构期，部分问题可能已被识别"
- **网络不可用**: web_search 步骤失败时，跳过技术趋势和组件特性搜索，基于已知知识输出报告，并在报告中标注 "
  网络不可用，组件特性分析基于本地知识库"

---

## 工作区路径

- **项目根目录**: `/home/carlos/.openclaw/agents/carlos-dev-assistant/workspace/carlos-framework`
- **报告输出目录**: `reports/`
- **编码规范参考**: `.claude/skills/carlos-framework-standard/SKILL.md`
- **错误码规范参考**: `carlos-commons/carlos-spring-boot-core/ERROR_CODE_SPEC.md`
- **所有分析基于此目录下的实际代码，严禁虚构文件路径或代码内容**
